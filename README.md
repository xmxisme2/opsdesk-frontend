# OpsDesk Frontend

OpsDesk 前端工程，使用 Vue 3、TypeScript、Vite、Element Plus、Pinia、Vue Router、Axios 和 ECharts。

## 开发命令

```bash
npm install
npm run dev
npm run build
```

## 当前状态

前端已完成初始化，包含基础目录、路由守卫、权限菜单、HTTP 封装、Store、布局和页面占位骨架。正式页面开发前需要先按 `../docs/figma-links.md` 读取对应 Figma Frame。
